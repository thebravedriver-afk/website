import Contact from "@/components/Contact";

export const metadata = {
  title: "Contact & Booking | Brave Driver Tours & Travels",
  description: "Schedule a consultation or book your next trip with Brave Driver Tours & Travels.",
};

export default function ContactPage() {
  return <Contact />;
}
